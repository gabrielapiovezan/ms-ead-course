server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ead_course
    username: user-authuser
    password: '123456'
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true

        dialect: org.hibernate.dialect.MySQL8Dialect